mutation GameEngineCreate($name: String!, $description: String, $content: Upload!, $ruleset: Upload) {
  gameEngineCreate(name: $name, description: $description, content: $content, ruleset: $ruleset) {
    isSuccess
    error
    engine {
      ...GameEngine
    }
  }
}
