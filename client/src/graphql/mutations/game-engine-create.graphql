mutation GameEngineCreate($name: String!, $description: String, $engine: Upload!, $ruleset: Upload) {
  gameEngineCreate(name: $name, description: $description, engine: $engine, ruleset: $ruleset) {
    isSuccess
    error
    engine {
      ...GameEngine
    }
  }
}
