query GetGameDetails($gameId: ID!) {
  node(id: $gameId) {
    ... on Game {
      id
      name
      lastTurnNumber
      me {
        id
      }
      players(quit: false) {
        items {
          id
          name
          number
          isClaimed
          nextTurn {
            isReady
            isOrdersSubmitted
            isTimesSubmitted
          }
        }
      }
    }
  }
}
